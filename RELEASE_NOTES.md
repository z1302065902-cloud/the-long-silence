# The Long Silence · 发布说明

> 版本号：2026.08.04 构建
> 部署地址：https://z1302065902-cloud.github.io/the-long-silence/

---

## 一、本轮改动总览

本轮包含三大部分：**游戏性打磨（9 项）**、**数值平衡（4 项）**、**新内容（3 个功能方向）**。
前两部分已随 `e199c1d` 提交并部署；新内容已完成、构建验证通过，待推送。

---

## 二、游戏性打磨（已发布）

1. **鼠标灵敏度设置生效** —— 设置面板的"鼠标灵敏度"滑杆此前只保存数值、不生效；现在实时控制转向灵敏度，且启动时读取已保存的数值。
2. **飞船双尾焰对称** —— 修复尾焰只从右侧喷口渲染的不对称问题，左右喷口均可见。
3. **E 键按模式分离** —— 飞行时 E 为横滚，落地/进站后 E 作为互动键（精炼货物），消除双语义冲突。
4. **精炼收益闭环** —— 空间站精炼晶体现在会转化为机库积分（每单位 8 积分），并播放拾取音效；形成"采矿 → 精炼 → 买船"正反馈。
5. **过关回血收敛** —— 通关时立即护盾回满、船体 +40，逻辑统一到一处，时机对玩家更友好。
6. **音频监听去重** —— 修复音频 keep-alive 监听器在每次播放时重复注册的问题。
7. **机库/HUD 占位文本修正** —— 首屏占位改为真实第一艘船 Bob，HUD 初始 HP/护盾改为与实际一致（160/90）。
8. **autopilot 初始值** —— 测试 AI 的初始血量改为 Infinity，避免误报"重火力命中"。
9. **瞄准手感** —— 辅助瞄准强度从 0.85 微调至 0.72，狗斗时需要多一点真实瞄准。

## 三、数值平衡（已发布）

针对后期难度陡增的四项低风险调参：

| 项目 | 改动 | 效果 |
|---|---|---|
| 每波敌数封顶 | `2 + i/3` → `min(6, …)` | L20 每波从 8 只降为 6 只 |
| 敌血增幅放缓 | `t×1.8` → `t×1.35` | L20 敌血从 2.8 倍降为 2.35 倍 |
| Boss 血量线性化 | `420 + i×95 + i²×6` → `420 + i×140` | L20 Boss 从约 4625 降为约 3220 |
| 等离子伤害提升 | `28` → `40` | 缩小等离子与脉冲的 DPS 差距，武器选择更丰富 |

## 四、新内容（构建通过，待推送）

### 1. 触屏操控（移动端适配）
- **虚拟摇杆**：屏幕左侧，拖动控制前进/后退/平移，摇杆圆点实时跟随。
- **动作按钮**：右下角 开火、导弹、互动、返回 四个圆形按钮。
- **拖拽转向**：在屏幕空白处拖动旋转视角，灵敏度随设置联动。
- **仅触屏显示**：`pointer: coarse` 设备才显示触控层，桌面端不受影响。
- **触摸不再抢 pointer lock**：避免手机浏览器锁定屏幕。

### 2. 新武器「布雷器 MINE LAYER」
- 第 6 号武器（数字键 1–6 选择）。
- 发射静止近爆雷，存在 6 秒；敌人进入 30 单位范围即触发 AoE 爆炸。
- 3D 尖刺弹体 + 脉冲光环，带专属部署音效。
- 配套新升级「雷管增装」：布雷器爆炸伤害 +25%。

### 3. 新敌人「神风机 Rage Runner」
- 高速直冲玩家，不做任何射击。
- 接触玩家即自爆，造成 60×难度伤害；被击落同样计分。
- 压缩交战距离，为后期战斗增加紧迫感。

### 4. 音效增强
- **Boss 出场音效**：低音渐强 + 金属撞击的警示音，Boss 生成时触发。
- **布雷部署音效**：气动声。

## 五、画面质感增强（深度提升）

1. **电影调色后处理** —— 新增暗角（聚焦画面中心）、轻微色差（镜头感）、饱和度与对比度微调，整体更接近电影质感。
2. **星云动态化** —— 背景星云从静止改为缓慢旋转 + 呼吸脉动 + 上下漂移，深空更有"活"的感觉。
3. **行星纹理提质** —— 程序化星球纹理从 512 提升到 1024 分辨率，近距离更清晰。
4. **飞船材质增强** —— 船体/点缀/玻璃的环境反射强度提升，金属反光更有层次。
5. **环境反射重构** —— 反射环境改为暖阳 + 冷边光 + 顶部天光 + 底部星云色四向组合，金属船体反射出有层次的渐变。

---

## 六、技术备注

- 构建通过：`tsc && vite build`（46 模块，无错误）。
- 唯一的构建警告为 three.js 独立 chunk 超过 500KB，属预期（three.js 被手动拆分到独立文件以优化首屏加载）。
- 本地仓库可能先于 GitHub main 的改动（如胶片颗粒、光标随动飞行辅助）均已保留。

---

## 七、本地验证清单

- [ ] 设置面板拖动"鼠标灵敏度"→ 转向速度变化
- [ ] 机库/飞行时飞船左右对称尾焰
- [ ] 落地/进站后 E 为互动；飞行时 E 为横滚
- [ ] 空间站按 E 精炼 → 机库积分增加
- [ ] 打一关 → 过关立即护盾回满、船体 +40
- [ ] 切换到布雷器放雷，观察敌入范围爆炸
- [ ] 遭遇神风机，观察冲锋自爆
- [ ] Boss 出场有警示音效
- [ ] 手机浏览器：摇杆移动、拖拽转向、按钮触发
- [ ] 机库中观察飞船金属反光质感
- [ ] 深空飞行观察星云缓慢流动
- [ ] 画面四周有柔和暗角，边缘有轻微色差
